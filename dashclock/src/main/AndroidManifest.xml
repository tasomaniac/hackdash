<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tasomaniac.hackerspace.status">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:debuggable="false"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="com.tasomaniac.hackerspace.status.ChooseHackerSpaceActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="Choose Your HackerSpace"
            android:theme="@android:style/Theme.Holo.Light.Dialog">
        </activity>

        <receiver android:name="com.tasomaniac.hackerspace.status.StatusToastReceiver">
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE"/>
                <data android:scheme="android_secret_code" android:host="47"/>
            </intent-filter>
        </receiver>

        <service
            android:name="com.tasomaniac.hackerspace.status.StatusService"
            android:icon="@drawable/ic_hackerspace"
            android:label="@string/app_name"
            android:permission="com.google.android.apps.dashclock.permission.READ_EXTENSION_DATA">
            <intent-filter>
                <action android:name="com.google.android.apps.dashclock.Extension" />
            </intent-filter>
            <meta-data android:name="protocolVersion" android:value="2" />
            <meta-data android:name="worldReadable" android:value="true" />
            <meta-data android:name="description"
                android:value="@string/app_description" />
            <!-- A settings activity is optional -->
            <meta-data android:name="settingsActivity"
                android:value=".ChooseHackerSpaceActivity" />
        </service>
    </application>

</manifest>
